<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maritocx.pideloapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.RegistroUNO" />
        <activity android:name=".forgetPass" />
        <activity android:name=".Admin.RegistroDOS" />
        <activity android:name=".Admin.AdminUNO" />
        <activity android:name=".Admin.RegistroTRES" />
        <activity android:name=".Cliente.ClienteUNO" />
        <activity android:name=".Cliente.ClienteDOS" />
        <activity android:name=".Cliente.ClienteTRES"></activity>
    </application>

</manifest>